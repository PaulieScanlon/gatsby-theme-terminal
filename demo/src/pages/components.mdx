---
navigationLabel: Components
---

## gatsby-theme-terminal

gatsby-theme-terminal has some of it's own components, and below is how you use them.

## SourceList
By default the source list returns all `.mdx` found from the directories defined in `gatsby-config.js`

```javascript
<SourceList>
  {source => (
    <ul>
      {source.map((edge, index) => {
        const {
          frontmatter: { title },
        } = edge.node
        return <li key={index}>{title}</li>
      })}
    </ul>
  )}
</SourceList>
```

<SourceList>
  {source => (
    <ul>
      {source.map((edge, index) => {
        const {
          frontmatter: { title },
        } = edge.node
        return <li key={index}>{title}</li>
      })}
    </ul>
  )}
</SourceList>

<Divider />

### filter

You can filter the data returned by the `<SourceList />` component by defining a source with the `filter` prop.

*NOTE:* the `filter` source _must_ be one from your `gatsby-config.js`


```javascript
<SourceList filter='posts'>
  {source => (
    <ul>
      {source.map((edge, index) => {
        const {
          frontmatter: { title },
        } = edge.node
        return <li key={index}>{title}</li>
      })}
    </ul>
  )}
</SourceList>
```

<SourceList filter='posts'>
  {source => (
    <ul>
      {source.map((edge, index) => {
        const {
          frontmatter: { title },
        } = edge.node
        return <li key={index}>{title}</li>
      })}
    </ul>
  )}
</SourceList>

<Divider />

## TagList
By default the tag list returns all tags found in the `.mdx` sourced from the directories defined in `gatsby-config.js`

```javascript
<TagList>
  {source => (
    <Flex
    sx={{flexDirection: 'column'}}
    >
      {
        source.map((tag, index) => {
          const {name, count, percent} = tag
          return <Box  key={index}>
            {`${name}  x${count}`}
            <Box mb={2}  bg='muted' sx={{width: `${percent}%`, height: 4}} />
          </Box>
        })
      }
    </Flex>
  )}
</TagList>
```

<TagList>
  {source => (
    <Flex
    sx={{flexDirection: 'column'}}
    >
      {
        source.map((tag, index) => {
          const {name, count, percent} = tag
          return <Box  key={index}>
            {`${name}  x${count}`}
            <Box mb={2}  bg='muted' sx={{width: `${percent}%`, height: 4}} />
          </Box>
        })
      }
    </Flex>
  )}
</TagList>

<Divider />

### filter

You can filter the tags returned by the `<TagList />` component by defining a source with the `filter` prop.

*NOTE:* the `filter` source _must_ be one from your `gatsby-config.js`

```javascript
<TagList filter='posts'>
  {source => (
    <Flex
    sx={{flexDirection: 'column'}}
    >
      {
        source.map((tag, index) => {
          const {name, count, percent} = tag
          return <Box  key={index}>
            {`${name}  x${count}`}
            <Box mb={2}  bg='muted' sx={{width: `${percent}%`, height: 4}} />
          </Box>
        })
      }
    </Flex>
  )}
</TagList>
```

<TagList filter='posts'>
  {source => (
    <Flex
    sx={{flexDirection: 'column'}}
    >
      {
        source.map((tag, index) => {
          const {name, count, percent} = tag
          return <Box  key={index}>
            {`${name}  x${count}`}
            <Box mb={2}  bg='muted' sx={{width: `${percent}%`, height: 4}} />
          </Box>
        })
      }
    </Flex>
  )}
</TagList>

<Divider />

## theme-ui/components

gatsby-theme-terminal also supports the full set of [theme-ui/componets](https://theme-ui.com/components) and below is how you use them.

If you'd like to see the default theme `.js` that file can be found in [here](https://github.com/PaulieScanlon/gatsby-theme-terminal/blob/master/%40pauliescanlon/gatsby-theme-terminal/src/gatsby-plugin-theme-ui/index.js)

## Box

```javascript
<Box p={4} color="white" bg="primary">
  Beep
</Box>
```

<Box p={4} color="white" bg="primary">
  Beep
</Box>

<Divider />

## Flex

```javascript
<Flex>
  <Box p={2} bg='primary' sx={{ flex: '1 1 auto' }}>
    Flex
  </Box>
  <Box p={2} bg='muted'>
    Box
  </Box>
</Flex>
```
<Flex>
  <Box p={2} bg='primary' sx={{ flex: '1 1 auto' }}>
    Flex
  </Box>
  <Box p={2} bg='muted'>
    Box
  </Box>
</Flex>

<Divider />

## Buttons 

```javascript
<Button mr={2}>primary</Button>
<Button mr={2} variant='secondary'>secondary</Button>
<Button mr={2} variant='success'>success</Button>
<Button variant='error'>error</Button>
```
<Button mr={2}>primary</Button>
<Button mr={2} variant='secondary'>secondary</Button>
<Button mr={2} variant='success'>success</Button>
<Button variant='error'>error</Button>


<Divider />

## Text

```javascript
<Text
  sx={{
    fontWeight: 'bold',
  }}>
  Hello
</Text>
<Text variant='caps'>Hello</Text>
```

<Text
  sx={{
    fontWeight: 'bold',
  }}>
  Hello
</Text>
<Text variant='caps'>Hello</Text>

<Divider />

## Heading

```javascript
<Heading variant='styles.h1'>h1</Heading>
<Heading variant='styles.h2'>h2</Heading>
<Heading variant='styles.h3'>h3</Heading>
<Heading variant='styles.h4'>h4</Heading>
<Heading variant='styles.h5'>h5</Heading>
<Heading variant='styles.h6'>h6</Heading>
```

<Heading variant='styles.h1'>h1</Heading>
<Heading variant='styles.h2'>h2</Heading>
<Heading variant='styles.h3'>h3</Heading>
<Heading variant='styles.h4'>h4</Heading>
<Heading variant='styles.h5'>h5</Heading>
<Heading variant='styles.h6'>h6</Heading>


## Link

```javascript
<Link href='#!'>Hello</Link>
```

<Link href='#!'>Hello</Link>


<Divider />

## Badge

```javascript
<Badge variant='primary' mb={2} mr={2}>
  Beep boop, this is an badge!
</Badge>

<Badge variant='secondary' mb={2} mr={2}>
  Beep boop, this is an badge!
</Badge>

<Badge variant='success' mb={2} mr={2}>
  Beep boop, this is an badge!
</Badge>

<Badge variant='error'>
  Beep boop, this is an badge!
</Badge>
```

<Badge variant='primary' mb={2} mr={2}>
  Beep boop, this is an badge!
</Badge>

<Badge variant='secondary' mb={2} mr={2}>
  Beep boop, this is an badge!
</Badge>

<Badge variant='success' mb={2} mr={2}>
  Beep boop, this is an badge!
</Badge>

<Badge variant='error'>
  Beep boop, this is an badge!
</Badge>


<Divider />

## Image

```javascript
  <Image src='https://placekitten.com/g/400/300' />
```

<Image src='https://placekitten.com/g/400/300' />

<Divider />

## Card

```javascript
<Card
  sx={{
    maxWidth: 400,
  }}>
  <Image src='https://placekitten.com/g/800/600' />
  <Box p={3}>
  <Heading variant='styles.h4'>Heading</Heading>
  <Text>Text</Text>
  </Box>
</Card>
```

<Card
  sx={{
    maxWidth: 400,
  }}>
  <Image src='https://placekitten.com/g/800/600' />
  <Box p={3}>
  <Heading variant='styles.h4'>Heading</Heading>
  <Text>Text</Text>
  </Box>
</Card>


<Divider />

## Alert

```javascript
<Alert variant='primary' mb={2}>
  Beep boop, this is an alert!
</Alert>

<Alert variant='secondary' mb={2}>
  Beep boop, this is an alert!
</Alert>

<Alert variant='success' mb={2}>
  Beep boop, this is an alert!
</Alert>

<Alert variant='error'>
  Beep boop, this is an alert!
</Alert>
```

<Alert variant='primary' mb={2}>
  Beep boop, this is an alert!
</Alert>

<Alert variant='secondary' mb={2}>
  Beep boop, this is an alert!
</Alert>

<Alert variant='success' mb={2}>
  Beep boop, this is an alert!
</Alert>

<Alert variant='error'>
  Beep boop, this is an alert!
</Alert>
